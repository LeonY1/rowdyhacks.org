import styled from "styled-components";

const SectionWrapper = styled.section`
  display: block;
  padding-top: 70px;
  margin: 0px 100px;
  text-align: left;
`;

const TitleWrapper = styled.h1`
  font-size: 36px;
  font-weight: bold;
`;

const TextWrapper = styled.p``;

export { SectionWrapper, TitleWrapper, TextWrapper };
